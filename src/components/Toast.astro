<div
  class="toast fixed bottom-5 left-1/2 -translate-x-1/2 z-[100] bg-surface2 border border-line text-ink font-mono text-[13px] px-[18px] py-2.5 rounded-[10px]"
  :class="{ show: $store.toast.visible }"
  x-text="$store.toast.msg"
>
</div>
